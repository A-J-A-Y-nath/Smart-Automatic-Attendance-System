<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.SmartAttendance" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="colorPrimaryContainer">@color/md_theme_light_primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/md_theme_light_onPrimaryContainer</item>
        <item name="android:windowBackground">@color/bg_gradient_start</item>
    </style>

    <style name="Theme.SmartAttendance" parent="Base.Theme.SmartAttendance" />

    <!-- Toolbar style -->
    <style name="AppToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/toolbar_bg</item>
        <item name="android:elevation">4dp</item>
    </style>

    <!-- Live badge text -->
    <style name="LiveBadge">
        <item name="android:background">@color/live_badge_bg</item>
        <item name="android:textColor">@color/live_badge_text</item>
        <item name="android:textSize">11sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingHorizontal">8dp</item>
        <item name="android:paddingVertical">3dp</item>
    </style>

    <!-- Scan button style -->
    <style name="ScanButton" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/scan_button_bg</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:letterSpacing">0.05</item>
    </style>
</resources>